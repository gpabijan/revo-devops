apiVersion: apps/v1
kind: Deployment
metadata:
  name: revolut-devops-tool
  labels:
    app: revolut-devops-tool
spec:
  selector:
    matchLabels:
      app: revolut-devops-tool
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: revolut-devops-tool
    spec:
      containers:
        - name: revolut-devops
          image: eu.gcr.io/revolut-devops/app:latest
          imagePullPolicy: Always
          env:
            - name: NODE_ENV
              value: "development"
            - name: DATABASE_NAME
              value: "ceybfpef"
#            - name: KAFKA_CONNECTION
#              valueFrom:
#                configMapKeyRef:
#                  name: kafka.connection
#                  key: kafka_bootstrap_servers_sasl_plain
#            - name: KAFKA_SASL_USERNAME
#              valueFrom:
#                secretKeyRef:
#                  name: kafka-credentials
#                  key: kafka_username
#            - name: KAFKA_SASL_PASSWORD
#              valueFrom:
#                secretKeyRef:
#                  name: kafka-credentials
#                  key: kafka_password
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /_ah/health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            successThreshold: 1
